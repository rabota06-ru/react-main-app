query GetSimilarResumes($fieldOfActivity: Int!, $currentResumeId: String!) {
  resumes(where: { fieldOfActivity: { equals: $fieldOfActivity }, id: { not: { equals: $currentResumeId } } }) {
    id
    firstname
    lastname
    fieldOfActivity
    placeOfResidence
    desiredPost
  }
}
