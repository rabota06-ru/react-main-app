query GetSimilarResumes($fieldOfActivity: Int!) {
  resumes(where: { fieldOfActivity: { equals: $fieldOfActivity } }) {
    id
    firstname
    lastname
    fieldOfActivity
    placeOfResidence
    desiredPost
  }
}
