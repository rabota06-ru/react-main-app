query GetResume($resumeId: String!) {
  resume(where: { id: $resumeId }) {
    id
    firstname
    lastname
    createdAt
    placeOfResidence
    phoneHidden
    phone
    aboutMe
    experience
    desiredPost
    education
    views
    fieldOfActivity
  }
}
