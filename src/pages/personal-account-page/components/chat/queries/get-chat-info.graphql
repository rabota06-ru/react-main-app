# запросить информацию по текущему чату
query GetChatInfo($chatId: String!) {
  chat(where: { id: $chatId }) {
    employer {
      companyName
    }
    applicant {
      resume {
        firstname
        lastname
      }
    }
    _count {
      messages
    }
  }
}
